/* =========================================================
   CONFIGURACIÓN — Repositorio de GitHub
   ---------------------------------------------------------
   Aquí va SOLO la dirección de tu repositorio (no es secreto).
   Cámbialo si tu usuario o el nombre del repo son distintos.
   El "token" de cada editor NO se pone aquí: se introduce en
   el panel admin.html y se guarda en su propio navegador.
   ========================================================= */
window.GFAA_GITHUB = {
  owner:  "marionadal",        // tu usuario de GitHub
  repo:   "gfaa-interno",      // nombre del repositorio
  branch: "main",              // rama (normalmente main)
  ruta:   "datos/contenidos.json"
};
